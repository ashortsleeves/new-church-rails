<%= form_for(@post) do |f| %>
 <%= javascript_include_tag Ckeditor.cdn_url %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" style="p: color: white;">
    <%= f.label :title %>
    <%= f.text_field :title %><br>
  </div>
  <div class="field">
    <%= f.label :body %>
    <%= f.cktext_area :body %>
  </div>
  <div class="actions">
    <%= f.submit :class => "button special formbutton" %>
    <%= link_to 'Cancel', posts_path,
            data: { confirm: 'Your changes will not be saved!'},
            :class => "button special cancelbutton" %>
  </div>

<% end %>
